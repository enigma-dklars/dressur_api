{% extends 'baseAdmin.html.twig' %}

{% block title %}Campagne — Attirer de nouveaux utilisateurs{% endblock %}

{% block body %}

<div class="d-flex align-items-center mb-3">
    <h4 class="mb-0"><i class="fas fa-paper-plane me-2 text-primary"></i>Attirer de nouveaux utilisateurs</h4>
    <a href="{{ path('app_communication_mail_portal') }}" class="btn btn-sm btn-outline-secondary ms-auto">
        <i class="fas fa-arrow-left me-1"></i>Retour au portail
    </a>
</div>

{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
            {{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    {% endfor %}
{% endfor %}

<div class="row g-4">

    {# ── Colonne gauche : saisie des adresses ── #}
    <div class="col-md-5">
        <div class="card h-100">
            <div class="card-header fw-semibold">
                <i class="fas fa-at me-1"></i>Adresses destinataires
            </div>
            <div class="card-body">

                <form method="post" action="{{ path('app_communication_mail_campagne_prospect') }}">

                    <div class="mb-3">
                        <label class="form-label small fw-semibold">Sujet du mail</label>
                        <p class="mb-0 border rounded px-2 py-2 small fw-semibold">{{ sujet }}</p>
                    </div>

                    <div class="mb-3">
                        <label class="form-label small fw-semibold">Reply-to</label>
                        <p class="mb-0 border rounded px-2 py-2 small font-monospace">{{ replyto }}</p>
                    </div>

                    <div class="mb-3">
                        <label for="emails" class="form-label fw-semibold">
                            Adresses mail <span class="text-danger">*</span>
                        </label>
                        <textarea
                            id="emails"
                            name="emails"
                            class="form-control font-monospace"
                            rows="10"
                            placeholder="exemple@mail.com, autre@mail.com&#10;encore@mail.com"
                            required
                        ></textarea>
                        <div class="form-text">
                            Séparateurs : virgule, point-virgule, espace, retour à la ligne.<br>
                            Doublons et adresses invalides ignorés automatiquement.<br>
                            Chaque adresse est conservée dans la base de prospects.
                        </div>
                    </div>

                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-paper-plane me-1"></i>Valider et mettre en file d'attente
                        </button>
                    </div>

                </form>
            </div>
        </div>
    </div>

    {# ── Colonne droite : aperçu du mail ── #}
    <div class="col-md-7">
        <div class="card h-100">
            <div class="card-header fw-semibold">
                <i class="fas fa-eye me-1"></i>Aperçu du mail envoyé
            </div>
            <div class="card-body p-0" style="background:#f8f9fa;">
                <div class="p-3">
                    {{ contentmail|raw }}
                </div>
            </div>
        </div>
    </div>

</div>

{% endblock %}
