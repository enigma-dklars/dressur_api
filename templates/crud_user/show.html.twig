{% extends 'baseAdmin.html.twig' %}

{% block title %}Utilisateur : {{ u.pseudo|default('Sans pseudo') }}{% endblock %}

{% block body %}
<div class="row g-2 mb-3 align-items-center">
    <div class="col"><p class="h4 text-info mb-0">Fiche utilisateur</p></div>
    <div class="col-auto"><a class="btn btn-sm btn-primary" href="{{ path('app_crud_user_index') }}">Retour à la liste</a></div>
</div>

<hr>
{{ include('crud_user/_user_details.html.twig') }}
{% endblock %}
