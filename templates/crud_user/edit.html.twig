{% extends 'baseAdmin.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <span class="h4 me-3">Edit User</span>
    <a class="btn btn-sm btn-primary h4" href="{{ path('app_crud_user_index') }}">back to list</a>

    {# {{ include('crud_user/_delete_form.html.twig') }} #}

    {{ include('crud_user/_form.html.twig', {'button_label': 'Update'}) }}
{% endblock %}
