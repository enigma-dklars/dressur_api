<form method="post" action="{{ path('app_env_paiement_api_delete', {'id': env_paiement_api.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ env_paiement_api.id) }}">
    {# <a href="{{ path('app_env_paiement_api_show', {'id': env_paiement_api.id}) }}" class="btn btn-sm btn-info">Show</a> #}
    <a href="{{ path('app_env_paiement_api_edit', {'id': env_paiement_api.id}) }}" class="btn btn-sm btn-success">Edit</a>
    <button class="btn btn-sm btn-danger">Delete</button>
</form>
