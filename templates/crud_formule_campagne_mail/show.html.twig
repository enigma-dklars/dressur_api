{% extends 'basePrivate.html.twig' %}

{% block title %}FormuleCampagneMail{% endblock %}

{% block body %}
    <h1>FormuleCampagneMail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formule_campagne_mail.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ formule_campagne_mail.titre }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ formule_campagne_mail.prix }}</td>
            </tr>
            <tr>
                <th>Nombre_mail</th>
                <td>{{ formule_campagne_mail.nombreMail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_crud_formule_campagne_mail_index') }}">back to list</a>

    <a href="{{ path('app_crud_formule_campagne_mail_edit', {'id': formule_campagne_mail.id}) }}">edit</a>

    {{ include('crud_formule_campagne_mail/_delete_form.html.twig') }}
{% endblock %}
