{% extends 'basePrivate.html.twig' %}

{% block title %}FormuleCampagneMail index{% endblock %}

{% block body %}
    <h1>FormuleCampagneMail index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Prix</th>
                <th>Nombre_mail</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for formule_campagne_mail in formule_campagne_mails %}
            <tr>
                <td>{{ formule_campagne_mail.id }}</td>
                <td>{{ formule_campagne_mail.titre }}</td>
                <td>{{ formule_campagne_mail.prix }}</td>
                <td>{{ formule_campagne_mail.nombreMail }}</td>
                <td>
                    <a href="{{ path('app_crud_formule_campagne_mail_show', {'id': formule_campagne_mail.id}) }}">show</a>
                    <a href="{{ path('app_crud_formule_campagne_mail_edit', {'id': formule_campagne_mail.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_crud_formule_campagne_mail_new') }}">Create new</a>
{% endblock %}
