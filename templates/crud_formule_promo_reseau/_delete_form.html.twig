<form method="post" action="{{ path('app_crud_formule_promo_reseau_delete', {'id': formule_promo_reseau.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formule_promo_reseau.id) }}">
    <button class="btn">Delete</button>
</form>
