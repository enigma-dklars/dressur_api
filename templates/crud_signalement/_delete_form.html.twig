<form method="post" action="{{ path('app_crud_signalement_delete', {'id': signalement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ signalement.id) }}">
    {# <a href="{{ path('app_crud_signalement_show', {'id': signalement.id}) }}" class="btn btn-sm btn-info">Show</a> #}
    {# <a href="{{ path('app_crud_signalement_edit', {'id': signalement.id}) }}" class="btn btn-sm btn-success">Edit</a> #}
    <button class="btn btn-sm btn-danger">Delete</button>
</form>
