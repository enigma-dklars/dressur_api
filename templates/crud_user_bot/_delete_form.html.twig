<form method="post" action="{{ path('app_crud_user_bot_delete', {'id': user_bot.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_bot.id) }}">
    <a href="{{ path('app_crud_user_bot_show', {'id': user_bot.id}) }}" class="btn btn-sm btn-info">Show</a>
    <a href="{{ path('app_crud_user_bot_edit', {'id': user_bot.id}) }}" class="btn btn-sm btn-success">Edit</a>
    <button class="btn btn-sm btn-danger">Delete</button>
</form>
