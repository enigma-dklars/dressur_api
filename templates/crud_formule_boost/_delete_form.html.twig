<form method="post" action="{{ path('app_crud_formule_boost_delete', {'id': formule_boost.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formule_boost.id) }}">
    <button class="btn">Delete</button>
</form>
