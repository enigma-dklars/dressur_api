{% extends 'basePrivate.html.twig' %}

{% block title %}MsgAi{% endblock %}

{% block body %}
    <h1>MsgAi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ msg_ai.id }}</td>
            </tr>
            <tr>
                <th>Recepteur</th>
                <td>{{ msg_ai.recepteur }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ msg_ai.message }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ msg_ai.createdAt ? msg_ai.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_crud_msg_ai_index') }}">back to list</a>

    <a href="{{ path('app_crud_msg_ai_edit', {'id': msg_ai.id}) }}">edit</a>

    {{ include('crud_msg_ai/_delete_form.html.twig') }}
{% endblock %}
