{% extends 'basePrivate.html.twig' %}

{% block title %}Edit DSBonus{% endblock %}

{% block body %}
    <span class="h4 me-3">Edit DSBonus</span>

    <a class="btn btn-sm btn-primary h4" href="{{ path('app_crud_d_s_bonus_index') }}">back to list</a>

    {# {{ include('crud_ds_bonus/_delete_form.html.twig') }} #}

    {{ include('crud_ds_bonus/_form.html.twig', {'button_label': 'Update'}) }}
{% endblock %}
