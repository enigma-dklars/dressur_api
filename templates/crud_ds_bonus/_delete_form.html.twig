<form method="post" action="{{ path('app_crud_d_s_bonus_delete', {'id': d_s_bonu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ d_s_bonu.id) }}">
    <button class="btn">Delete</button>
</form>
