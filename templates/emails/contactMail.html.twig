{% extends 'emails/templates_mail.html.twig' %}

{% block contenu_mail %}
    <div>
        <p>Objet :  {{objet}}</p>
        <p>Nom :  {{name}}</p>
        <p>Email :  {{email}}</p>
        <p>Message :  {{message|nl2br}}</p>
    </div>
{% endblock %}