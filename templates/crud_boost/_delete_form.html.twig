<form method="post" action="{{ path('app_crud_boost_delete', {'id': boost.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ boost.id) }}">
    <button class="btn">Delete</button>
</form>
