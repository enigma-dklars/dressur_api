<form method="post" action="{{ path('app_crud_promo_reseau_delete', {'id': promo_reseau.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ promo_reseau.id) }}">
    <button class="btn">Delete</button>
</form>
