<form method="post" action="{{ path('app_crud_suggestion_delete', {'id': suggestion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suggestion.id) }}">
    <a href="{{ path('app_crud_suggestion_show', {'id': suggestion.id}) }}" class="btn btn-sm btn-info">Show</a>
    <a href="{{ path('app_crud_suggestion_edit', {'id': suggestion.id}) }}" class="btn btn-sm btn-success">Edit</a>
    <button class="btn btn-sm btn-danger">Delete</button>
</form>
