<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-white rounded-0 border-bottom">
        <div class="container">
        <a class="navbar-brand" href="{{path('app_public')}}"><img src="/assets/images/brand-logo-2.png" width="140" alt=""/></a>
        <button class="navbar-toggler border border-2 border-primary text-primary px-3 py-2" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="fas fa-list-ul fs-5"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {% if app.request.requestUri == path('app_public') %}text-primary{% else %}text-dark{% endif %} fw-semibold" href="{{path('app_public')}}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.request.requestUri == path('app_actualite') %}text-primary{% else %}text-dark{% endif %} fw-semibold" href="{{path('app_actualite')}}">Actualités</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link {% if app.request.requestUri in [path('app_dressur_bot'), path('app_boost_contact'), path('app_campagne_mail'), path('app_promotion_affaire'), path('app_carte_visite_numerique'), path('app_promotion_reseaux_sociaux')] %}text-primary{% else %}text-dark{% endif %} fw-semibold dropdown-toggle dropdown-toggle-nocaret" href="#" data-bs-toggle="dropdown"> 
                        Services <i class="fas fa-chevron-down align-middle ms-2"></i>
                    </a>
                    <ul class="dropdown-menu bg-white border border-2 border-primary">
                        <li><a href="{{path('app_dressur_bot')}}" class="dropdown-item text-dark" style="color: #2a4b9a !important; font-weight: 500;"><i class="fas fa-align-left me-2"></i> Dressur Bot</a></li>
                        <li><a href="{{path('app_boost_contact')}}" class="dropdown-item text-dark" style="color: #2a4b9a !important; font-weight: 500;"><i class="fas fa-align-left me-2"></i> Boost Contact</a></li>
                        <li><a href="{{path('app_campagne_mail')}}" class="dropdown-item text-dark" style="color: #2a4b9a !important; font-weight: 500;"><i class="fas fa-align-left me-2"></i> Campagne Mail</a></li>
                        <li><a href="{{path('app_promotion_affaire')}}" class="dropdown-item text-dark" style="color: #2a4b9a !important; font-weight: 500;"><i class="fas fa-align-left me-2"></i> Promotion Affaire</a></li>
                        <li><a href="{{path('app_carte_visite_numerique')}}" class="dropdown-item text-dark" style="color: #2a4b9a !important; font-weight: 500;"><i class="fas fa-align-left me-2"></i> Carte de Visite Numérique</a></li>
                        <li><a href="{{path('app_promotion_reseaux_sociaux')}}" class="dropdown-item text-dark" style="color: #2a4b9a !important; font-weight: 500;"><i class="fas fa-align-left me-2"></i> Promotions Réseaux Sociaux</a></li>
                        {# <li><hr class="dropdown-divider"></li> #}
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.request.requestUri == path('app_tarifs') %}text-primary{% else %}text-dark{% endif %} fw-semibold" href="{{path('app_tarifs')}}">Tarifs</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.request.requestUri == path('app_contactez_nous') %}text-primary{% else %}text-dark{% endif %} fw-semibold" href="{{path('app_contactez_nous')}}">Contacts</a>
                </li>
            </ul>

            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                
            </ul>
            <div class="d-flex gap-2">
                <div hidden>
                    fas fa-moon
                    fas fa-sun
                </div>
                <div class="notifications change-theme">
                    <i class="fas {% if theme == "dark-theme" %}fa-moon{% else %}fa-sun{% endif %} ici-theme"></i>
                </div>
                {% if is_connect == "oui" %}
                    <a href="{{path('app_private')}}" class="btn btn-success btn-sm px-3 py-2 radius-30"><i class="fas fa-house-user me-2"></i>Dashboard</a>
                {% else %}
                    <a href="{{path('app_connexion')}}" class="btn {% if app.request.requestUri == path('app_connexion') %}btn-primary{% else %}btn-white{% endif %} btn-sm px-3 py-2 radius-30"><i class="fas fa-arrow-right-to-bracket me-2"></i>Connexion</a>
                {% endif %}
                <a href="{{path('app_inscription')}}" class="btn {% if app.request.requestUri == path('app_inscription') %}btn-primary{% else %}btn-white{% endif %} btn-sm px-3 py-2 radius-30"><i class="fas fa-user-plus me-2"></i>Inscription</a>
            </div>
        </div>
        </div>
    </nav>
</header>