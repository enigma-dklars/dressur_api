<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-white rounded-0 border-bottom">
        <div class="container">
        <a class="navbar-brand" href="{{path('app_public')}}"><img src="assets/images/brand-logo-2.png" width="140" alt=""/></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mb-2 mb-lg-0 align-items-center">
            <li class="nav-item">
                <a class="nav-link {% if app.request.requestUri == path('app_public') %}text-primary{% else %}text-dark{% endif %} fw-semibold" href="{{path('app_public')}}">Accueil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {% if app.request.requestUri == path('app_actualite') %}text-primary{% else %}text-dark{% endif %} fw-semibold" href="{{path('app_actualite')}}">Actualités</a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link text-dark fw-semibold dropdown-toggle dropdown-toggle-nocaret" href="#" data-bs-toggle="dropdown"> 
                    Services <i class="fas fa-chevron-down align-middle ms-2"></i>
                </a>
                <ul class="dropdown-menu bg-white border border-2 border-primary">
                    <li><a class="dropdown-item text-dark" href="#">Boost Contact</a></li>
                    <li><a class="dropdown-item text-dark" href="#">Campagne Mail</a></li>
                    <li><a class="dropdown-item text-dark" href="#">Promotion Affaire</a></li>
                    <li><a class="dropdown-item text-dark" href="#">Promotions Réseaux Sociaux</a></li>
                    {# <li><hr class="dropdown-divider"></li> #}
                </ul>
            </li>
            </ul>

            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
            <li class="nav-item">
                <a class="nav-link {% if app.request.requestUri == path('app_contactez_nous') %}text-primary{% else %}text-dark{% endif %} fw-semibold" href="{{path('app_contactez_nous')}}">Contacts</a>
            </li>
            </ul>
            <div class="d-flex ms-3 gap-3">
            <a href="{{path('app_connexion')}}" class="btn {% if app.request.requestUri == path('app_connexion') %}btn-primary{% else %}btn-white{% endif %} btn-sm px-3 py-2 radius-30"><i class="fas fa-arrow-right-to-bracket me-2"></i>Connexion</a>
            <a href="{{path('app_inscription')}}" class="btn {% if app.request.requestUri == path('app_inscription') %}btn-primary{% else %}btn-white{% endif %} btn-sm px-3 py-2 radius-30"><i class="fas fa-user-plus me-2"></i>Inscription</a>
            </div>
        </div>
        </div>
    </nav>
</header>