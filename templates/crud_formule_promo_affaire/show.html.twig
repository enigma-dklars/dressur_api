{% extends 'basePrivate.html.twig' %}

{% block title %}FormulePromoAffaire{% endblock %}

{% block body %}
    <h1>FormulePromoAffaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formule_promo_affaire.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ formule_promo_affaire.titre }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ formule_promo_affaire.prix }}</td>
            </tr>
            <tr>
                <th>NbrJour</th>
                <td>{{ formule_promo_affaire.nbrJour }}</td>
            </tr>
            <tr>
                <th>Alert</th>
                <td>{{ formule_promo_affaire.alert ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_crud_formule_promo_affaire_index') }}">back to list</a>

    <a href="{{ path('app_crud_formule_promo_affaire_edit', {'id': formule_promo_affaire.id}) }}">edit</a>

    {{ include('crud_formule_promo_affaire/_delete_form.html.twig') }}
{% endblock %}
