<div class="d-flex gap-1">
    <!-- Bouton Voir -->
    <a class="btn btn-sm btn-info" href="{{ path('app_historique_programme_recompense_show', {'id': historique_programme_recompense.id}) }}">
        Show
    </a>

    <!-- Bouton Modifier -->
    <a class="btn btn-sm btn-primary" href="{{ path('app_historique_programme_recompense_edit', {'id': historique_programme_recompense.id}) }}">
        Update
    </a>

    <!-- Formulaire Supprimer -->
    <form method="post" action="{{ path('app_historique_programme_recompense_delete', {'id': historique_programme_recompense.id}) }}"
          onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ historique_programme_recompense.id) }}">
        <button class="btn btn-sm btn-danger">Delete</button>
    </form>
</div>
